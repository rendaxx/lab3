defmodule MyLab3 do
  @moduledoc """
  Streaming interpolation lab entry points.

  The CLI is implemented in `MyLab3.CLI` and the process pipeline lives in
  `MyLab3.Pipeline`. Interpolation math is provided by modules under
  `MyLab3.Interpolation`.
  """
end
